# Base URL for the NelBac backend API.
# Defaults to http://localhost:8000/api/v1 when unset (see src/api/client.ts).
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Google OAuth client ID for Google Sign-In (GIS).
# Leave empty to hide the Google button entirely.
VITE_GOOGLE_CLIENT_ID=
